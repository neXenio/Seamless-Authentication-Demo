<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="me.seamless.authentication.example">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature
            android:name="android.hardware.bluetooth_le"
            android:required="true" />

    <application
            android:name="me.seamless.authentication.example.SampleApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true"
            tools:ignore="GoogleAppIndexingWarning">
        <activity
                android:name="me.seamless.authentication.example.overview.CommunicationUnitListActivity"
                android:label="@string/title_communication_unit_list"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
                android:name="me.seamless.authentication.example.detail.CommunicationUnitDetailActivity"
                android:exported="true"
                android:label="@string/title_communication_unit_detail"
                android:parentActivityName="me.seamless.authentication.example.overview.CommunicationUnitListActivity"
                android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="com.nexenio.seamlessauthenticationintegrationsample.me.nexenio.seamlessauthenticationintegrationsample.overview.CommunicationUnitListActivity" />
        </activity>

        <activity
                android:name="me.seamless.authentication.example.health.CommunicationUnitHealthActivity"
                android:exported="true"
                android:label="@string/title_communication_unit_health"
                android:parentActivityName="me.seamless.authentication.example.overview.CommunicationUnitListActivity"
                android:theme="@style/DarkFullscreen">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="com.nexenio.seamlessauthenticationintegrationsample.me.nexenio.seamlessauthenticationintegrationsample.overview.CommunicationUnitListActivity" />
        </activity>

    </application>

</manifest>